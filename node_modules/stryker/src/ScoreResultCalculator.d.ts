import { MutationScoreThresholds } from 'stryker-api/core';
import { MutantResult, ScoreResult } from 'stryker-api/report';
export default class ScoreResultCalculator {
    static calculate(results: MutantResult[]): ScoreResult;
    static determineExitCode(score: ScoreResult, thresholds: MutationScoreThresholds | undefined): void;
    private static wrapIfSingleFileScoreResult(scoreResult);
    private static calculateScoreResult(results, basePath);
    private static copy(defaults, overrides);
    private static determineFacts(basePath, results);
    private static compareScoreResults(a, b);
    private static calculateChildScores(results, parentName, basePath);
    private static determineCommonBasePath(results, basePath);
    private static countNumbers(mutantResults);
}
