import { StrykerOptions } from 'stryker-api/core';
import PromptOption from './PromptOption';
export default class StrykerConfigWriter {
    private out;
    private selectedTestFramework;
    private additionalPiecesOfConfig;
    private configObject;
    constructor(out: (output: string) => void, selectedTestRunner: null | PromptOption, selectedTestFramework: null | PromptOption, selectedReporters: PromptOption[], additionalPiecesOfConfig: Partial<StrykerOptions>[]);
    static guardForExistingConfig(): void;
    /**
    * Create stryker.conf.js based on the chosen framework and test runner
    * @function
    */
    write(): Promise<void>;
    private configureTestFramework();
    private writeStrykerConfig(configObject);
    private wrapInModule();
}
