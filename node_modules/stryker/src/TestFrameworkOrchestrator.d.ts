import { TestFramework } from 'stryker-api/test_framework';
import { StrykerOptions } from 'stryker-api/core';
export default class TestFrameworkOrchestrator {
    private options;
    constructor(options: StrykerOptions);
    determineTestFramework(): TestFramework | null;
    private determineFrameworkWithCoverageAnalysis();
    private informAboutKnownTestFrameworks();
    private createTestFramework(name);
    private testFrameworkExists(maybeFramework);
}
